<div class="space-y-6">
  <% conversation.messages.includes(:sender).ordered.each do |message| %>
    <%= render "messages/message",
      message: message,
      current_user: current_user %>
  <% end %>
</div>